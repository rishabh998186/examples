apiVersion: v1
kind: Service
metadata:
  name: analysis-app
  labels:
    app: analysis-app
spec:
  selector:
    app: analysis-app
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 80
  - name: metrics
    protocol: TCP
    port: 8080
    targetPort: 8080
  type: ClusterIP